# Dependencies
node_modules/

# Build output
client/dist/
server/dist/
shared/dist/
server/public/*
!server/public/.gitkeep

# Generated PWA icons (built from SVG via prebuild)
client/public/icons/*.png

# Database
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# User data
server/data/*
server/uploads/

# Compose bind mounts (created on first `docker compose up`)
/data/
/uploads/

# Environment
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.claude/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
*.pid
*.seed
*.pid.lock

# Coverage
coverage
coverage-*/
*.lcov
.nyc_output

# Cache
.npm
.eslintcache
.cache
*.tsbuildinfo
*.tgz

.scannerwork
test-data

# Live-MinIO/AIStor manual test license key — never commit, operators obtain their own free key
minio-licence.key

.run
.full-review

# Agent handoff notes — local working files, never ship these
HANDOFF-DOCUMENT.md
TT-HANDOFF.md

# Agent/session working files — never commit
.zcode/
.zcodeignore

# Throwaway Atlas geometry probes (ad-hoc investigation scripts, not tooling).
# Kept on disk so a running investigation is not lost, never committed.
removed_tmp.mjs
repro_region.mjs
